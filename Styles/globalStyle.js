import {
  StyleSheet,
} from 'react-native';

export const globalStyle = StyleSheet.create({
	backgroundColor: {
		backgroundColor: '#012172',
	},
	buttonBackgroud: {
		backgroundColor: '#0486DB', 
	}
})